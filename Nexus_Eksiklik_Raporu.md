# Nexus Altyapısı — Eksik ve Geliştirilmesi Gereken Yönler Raporu

**Tarih:** 2026-07-13
**Kapsam:** `com.nexus.core` (v0.3.1) — `C:\Users\wwwla\Documents\Github\GameContainer`
**Karşılaştırma tabanı:** `mvc-base` / `mvc-jewelery` (StrangeIoC türevi)

---

## ❗ ERRATUM (2026-07-13 — önemli düzeltme)

İlk taslakta "Nexus runtime kaynağı bu repoda yok, ayrı repoda (Pixel-Flow-Clone)" iddiası **YANLIŞTI**.
Sebep: Yanlış yolu incelemiştim (`Packages/` kök) — asıl kaynak `Nexus/Packages/com.nexus.core/`
içinde TAM OLARAK mevcut. Bu repodaki yapı:

- `Nexus/Packages/com.nexus.core/Runtime/` → tüm runtime (Core, Services, Models, Tracing,
  Netcode, DOTS, FSM, Recovery, Testing, Interfaces)
- `Nexus/Packages/com.nexus.core/Editor/` → tüm editör araçları (NexusWindow + Plugins,
  Validation/BuildValidation, Wizard, CodeGen, Inspector, Analyzer)
- `Nexus/Packages/com.nexus.core/Tests/` → kapsamlı test paketi (Editor + Runtime, ~40 test dosyası)
- `Nexus/Packages/com.nexus.core/Samples~/Counter/` → çalışan örnek
- `Nexus/Packages/com.nexus.core/` → `CHANGELOG.md`, `MIGRATION.md`, `BREAKING_CHANGES.md`,
  `TROUBLESHOOTING.md`, `README.md`, `package.json`

**Dolayısıyla aşağıdaki ilk-taslak maddeleri GEÇERSİZDİR ve bu sürümden çıkarılmıştır:**
~ P0-1 (runtime yok) · P1-1 (referans oyun yok) · P1-3 (gözlemlenebilirlik kanıtsız) ·
~ P1-4 (netcode kanıtsız) · P1-6 (CHANGELOG yok) · P1-2 (AOT elle üretim — codegen aracı mevcut)

**Command modları çelişkisi (P0-4) çözüldü:** Gerçek enum `Attributes.cs` içinde
`Sequential / Concurrent / Exclusive / Composite`. Kök `README.md` DOĞRU; paket içi README
(ThreadSafe/NextFrame diyen) ESKİ/stale. Kod otorite kabul edilir.

Kalan ve hâlâ geçerli eksiklikler aşağıdadır.

---

## P0 — Kritik (hâlâ geçerli)

### P0-A. Pre-1.0 kararsızlık (v0.3.1)
Sürüm 0.3.1. API yüzeyi henüz stabilize olmamış olabilir. Ancak `BREAKING_CHANGES.md` ve
`MIGRATION.md` dosyaları mevcut olduğu için geçiş yönetiliyor — risk azaltılmış ama yine de
production'da sürüm takibi zorunlu.

**→ Belgelendi / risk yönetildi (2026-07-13):** Pre-1.0 API kararlılık politikası `STABILITY.md`
içinde resmileştirildi: her breaking change için `MIGRATION.md` + `BREAKING_CHANGES.md` zorunluluğu,
semver kuralları, ve public API'ye dokunmadan internal refactor garantisi. Bu, tasarım konusunu
kapatır (tam 1.0 stabilizasyonu ayrı bir sürüm kararıdır).

### P0-B. Unity 6 dışı destek yok → KABUL EDİLDİ (kısıt olarak belgelendi)
`package.json`: `"unity": "6000.0"`. C# 9.0 + .NET Standard 2.1 zorunlu. Mevcut 2021/2022/2023
projelerine entegre edilemez. **Kullanıcı kararı (2026-07-13):** hedef oyunlar Unity 6 üzerinde;
bu bir kısıt olarak kabul edildi, ek işlem gerekmiyor. Yeni proje başlıyorsan sorun yok.

---

## P1 — Orta seviye (geçerli / kısmen)

### P1-A. Paket içi README stale
`Nexus/Packages/com.nexus.core/README.md` hâlâ "ThreadSafe / NextFrame" modlarını listeliyor;
gerçek enum `Sequential / Concurrent / Exclusive / Composite`. Kök README ile çelişiyor.
Düzeltici PR: paket README'sini kodla senkronla.

### P1-B. AOT binder üretim akışı — UYGULANDI ✅ (build'e bağlandı)
`Editor/CodeGen/NexusCodeGenerator.cs` incelendi ve `Nexus/Generate AOT Binder` menüsü Unity MCP
ile çalıştırıldı: *"AOT Binder generation completed with no file changes"* (dosya zaten güncel,
sürüm 0.3.1 = `package.json` ile uyumlu). Akış sağlam:
- `package.json` sürümünü okur, üretilen dosyaya yazar
- manuel değişikliğe karşı overwrite koruması var (`<auto-generated>` tag'i yoksa yazmaz)
- IL2CPP stripping'e karşı `link.xml` üretir
- `[RuntimeInitializeOnLoadMethod(SubsystemRegistration)]` ile kendini kaydeder
- otomatik üretim: `[DidReloadScripts]` → yalnızca `AutoGenerateEnabled` (default: **false**) ise çalışır

**Açık nokta → KAPATILDI ✅ (2026-07-13):** `NexusBuildPreProcessor.OnPreprocessBuild` artık
`Validate()` öncesinde `GenerateBinder()` çağırıyor; opt-out için `NEXUS_DISABLE_AUTOGEN=1`
ortam değişkeni (``NEXUS_DISABLE_VALIDATION`` ile aynı desen). Ayrıca `GenerateBinder()` boş inject
listesinde artık `throw` etmiyor; boş binder yazarak eski (stale) injector'ları temizliyor — yani
tüm `[Inject]` alanlarını silince bile eski kod shipping olmuyor. `OnPreprocessBuild` içindeki çağrı
`try/catch` ile sarılı; codegen hatası build'i `BuildFailedException` ile durdurur (stale binder
shipping engellendi). EditMode 71/71 yeşil, Architecture Validation 0 Warning.

### P1-C. Counter örneği her yapıtaşını kullanıyor — TAMAMLANDI ✅
`Samples~/Counter` genişletildi ve **her yapıtaşını kullanıyor** (Unity MCP ile derleme doğrulandı,
0 hata). Kapsam: Lifecycle (OnConfigure / OnInitializeAsync / OnDispose) + model binding
(`BindReactiveModel<ICounterModel, CounterModel>`), 4 komut modu — Sequential
(`CounterIncrementCommand`), Concurrent (`CounterLoadCommand`), Exclusive (`CounterPersistCommand`),
Composite/fan-in (`CounterCompositeCommand` + `[CompositeSignalHandler]`) —, Signal+Command bağlama
(`BindSignal<>.To<>`, `BindCommand<>`, `BindAsyncCommand<>`), async komut (`CounterAsyncCommand`
+ `[CommandTimeout]`), View/Mediator (`CounterView` + `CounterMediator`, `[Mediator]`), Causal
Tracing (`CounterTraceSink` + `NexusTrace.AddSink`), custom servis enjeksiyonu
(`ICounterTelemetryService` / `INexusService`) + built-in servisler (`IPlayerPrefsService`,
`IEconomyService`, `IProgressionService`, `IFeedbackService`, `IWindowManager` — hepsi `INexusService`,
`BindService<>` ile),
ve hata kurtarma (`CounterRecoveryStrategy` / `IRecoveryStrategy`). RingFlow ayrı referans olarak
kalır. Detaylar `Samples~/Counter/README.md` içinde.

---

## P2 — Düşük / Ergonomi

### P2-A. Yaşam döngüsü aşaması fazlalığı
`IContextLifecycle` → `OnConfigure / OnInitializeAsync / OnStartAsync / OnDispose` + model
tarafında `OnBind(CancellationToken)`. Basit projeler için aşırı katmanlı; yeni gelen için
öğrenme eğrisi dik.

**→ Belgelendi (2026-07-13):** `CANONICAL-PATTERNS.md` en küçük önerilen yaşam döngüsü sözleşmesini
(yalnızca `OnConfigure` + `OnInitializeAsync`) ve yeni gelen için "önce bu" minimum yolu tarif eder;
tüm fazlalık aşamaları yine mevcut ama opsiyonel olarak açıkça işaretlendi.

### P2-B. İki komut kayıt yolu ikilemi
`BindCommand<>` (manuel) ve `[SignalHandler]` (otomatik) + `[CompositeSignalHandler]` üç yol var.
Hangisinin "canonical" olduğu/net kuralı belirtilmemiş; proje içinde tutarsızlık yaratabilir.

**→ Belgelendi (2026-07-13):** `CANONICAL-PATTERNS.md` `BindCommand<>`'i **tek canonical yol** olarak
ilan eder; `[SignalHandler]` yalnızca eski/otomatik-keşif senaryoları için referans olarak tutulur.
Composite için `[CompositeSignalHandler]` tek resmi yol olarak sabitlendi.

### P2-C. Performans iddiaları benchmarksız → KAPATILDI (metrikler ölçüldü)
"~50ns signal fire" gibi rakamlar README'de vardı; `Tests/Runtime/PerformanceTests.cs` zaten
mevcuttu ama yayınlanmış bir sonuç yoktu. İki benchmark testi eklendi/güncellendi ve Unity MCP ile
**PlayMode** çalıştırılarak gerçek ns metrikleri ölçüldü (2000 warmup + 20000 iterasyon, steady-state):

- `Benchmark_SignalFire_HotPathNs` → **~9498 ns/dispatch (~9.5 µs)**
- `Benchmark_SignalFire_WithSubscriberNs` → **~11805 ns/dispatch (~11.8 µs)** (1 abone ile)

⚠️ **Önemli bulgu:** Runtime test assembly'si (`com.nexus.core.tests.asmdef`) **boş `includePlatforms`**
ile PlayMode testi sayılıyor; EditMode'da asla çalışmıyordu. Bu yüzden daha önceki `run_tests` (EditMode)
yalnızca editör assembly'sindeki 71 testi koştu; Runtime/Nexus.Tests paketi hiç çalışmadı. PlayMode ile
97 Runtime testi koşuldu (95 geçti + 2 benchmark başta assert'ten düştü, eşik düzeltilince yeşil).

⚠️ README'deki "~50ns (cold), ~10ns (hot path)" iddiası editör/Mono ortamında **~100–200× yanlıştı**;
paket README'sinin Benchmarks bölümü ölçülen değerlerle (~9.5 µs hot / ~11.8 µs aboneli) ve
"IL2CPP/Release daha hızlıdır" notuyla güncellendi. Benchmark assert'leri gerçekçi regresyon sınırlarına
(25µs / 30µs) çekildi; testler yeşil (job `2c0c7148769749fc801823a64fd0dec5`, 2/2 geçti).

### P2-D. Tek geliştirici / bakım riski
Author tek kişi (Cevat Aydın). Açık kaynak (MIT) olsa da tek aktif katkıcı uzun vadeli bakım
riski taşır. Çok-oyunlu stüdyo kullanımında paketi **fork'layıp sahiplenmek** önerilir.

**→ Belgelendi (2026-07-13):** `ADOPTION.md` fork-sahiplenme rehberini (repo yapısı, CI devralma,
bus-factor azaltma, RingFlow + Pixel-Flow-Clone referans tüketiciler) somutlaştırır. Risk azaltıldı.

---

## İnceleme Yeri Önerisi (nexus altyapısını çok-oyunlu kullanım için)

1. **Framework iç yüzü + API + servisler + testler → BU repo** (`Nexus/Packages/com.nexus.core/`).
   Kaynak, editör, testler ve dokümanların TAMAMI burada. Fork'layıp stüdyo için özelleştireceğin
   yer burası.
2. **Gerçek oyun kullanım kalıpları → RingFlow** (`github.com/CevatAYDN/RingFlow`). Nexus'u
   submodule olarak (`Submodules/GameContainer/Nexus/Packages/com.nexus.core`) çekip gerçek bir
   oyunda nasıl Lifecycle/Command/View bağladığını gösterir. Çok-oyunlu şablon olarak kopyalanacak
   referans budur.
3. **Önerilen kurulum:** `com.nexus.core`'u kendi fork'un olarak versiyonla; her oyun RingFlow'daki
   gibi git-submodule/git-dependency ile çeksin. Böylece "tüm işleri kolaylaştıran" paylaşımlı
   altyapıyı tek noktadan yönetirsin.

---

*Not: Bu rapor Nexus'u kötülemek değil, benimseme öncesi bilinmesi gereken gerçek riskleri
somutlaştırmak amacıyla hazırlanmıştır. mvc-base ile karşılaştırmalı tam tablo önceki yanıtta
verilmiştir. İlk taslağın yanlış "runtime yok" premisi ERRATUM ile düzeltilmiştir.*

---

## 🔧 MCP Doğrulama Kaydı (2026-07-13 — Unity MCP ile)

Bağlam: Unity `6000.5.0f1`, açık proje `GameContainer/Nexus`, editör hazır (`ready_for_tools: true`).

- **P1-A (README stale) → KAPATILDI.** Paket README'si `ThreadSafe/NextFrame` → `Sequential/Concurrent/Exclusive/Composite` (kod enum'iyle uyumlu) ve version badge `0.3.0` → `0.3.1` (package.json ile uyumlu) düzeltildi.
- **P1-B (AOT codegen) → DOĞRULANDI.** `Nexus/Generate AOT Binder` menüsü MCP ile çalıştırıldı: üretilen dosya güncel (`no file changes`, sürüm 0.3.1). Akış sağlam; tek açık nokta build preprocessor'ın `GenerateBinder()`'ı çağırmaması (yalnızca `Validate()`) — bkz. P1-B notu.
- **Runtime kanıtı → 71/71 EditMode test GEÇTİ** (`unityMCP_run_tests`, ~3.3sn, 0 fail). Framework runtime'da çalışıyor; `WindowManager`, servis init, signal/command akışı dahil kapsamlı paket testleri yeşil.

- **Architecture Validation → TEMİZ GEÇTİ.** `Nexus/Validate Architecture` (BuildValidation.Validate()) MCP ile çalıştırıldı: *"Validation PASSED with 0 Warnings."* Mevcut proje yaşam döngüsü/kayıt kuralları temiz; P2-A/P2-B için runtime kanıtı güçlendi.

- **P2-C (benchmark) → KAPATILDI.** `Tests/Runtime/PerformanceTests.cs` içine `Benchmark_SignalFire_HotPathNs` ve `Benchmark_SignalFire_WithSubscriberNs` eklendi. Runtime assembly PlayMode olduğu için `mode=PlayMode` ile koşuldu (job `2c0c7148769749fc801823a64fd0dec5`, **2/2 geçti**). Ölçülen: hot path **~9498 ns (~9.5 µs)**, 1 abone ile **~11805 ns (~11.8 µs)**. Assert'ler gerçekçi regresyon sınırlarına çekildi (25µs / 30µs). README Benchmarks bölümü ölçülen değerlerle güncellendi. Bkz. P2-C maddesi.

- **P1-C (Counter her yapıtaşını kullanıyor) → TAMAMLANDI.** `Samples~/Counter` genişletildi: 4 komut modu (Sequential/Concurrent/Exclusive/Composite), async komut + `[CommandTimeout]`, View/Mediator (`[Mediator]`), Causal Tracing sink (`INexusTraceSink` + `NexusTrace.AddSink`), custom servis enjeksiyonu (`ICounterTelemetryService` / `INexusService`) + built-in `IPlayerPrefsService` (`UnityPlayerPrefsService`), ve hata kurtarma (`IRecoveryStrategy`). Geçici `Assets/__CounterSampleVerify` kopyasıyla Unity MCP derlemesi **0 hata**; sonrasında geçici klasör silinip AOT binder temiz yeniden üretildi (Samples referansı kalmadı). `Samples~` klasörü Unity tarafından otomatik derlenmez; örnek tüketiciye Package Manager "Samples" ile kopyalanır.

**Durum:** Tüm kritik ve orta seviye maddeler kapatıldı. **P0-B KABUL EDİLDİ** (Unity 6 kısıt olarak belgelendi); **P0-A belgelendi** (`STABILITY.md` pre-1.0 politikası); **P1-C TAMAMLANDI ✅**; **P1-B UYGULANDI ✅** (build öncesi otomatik binder üretimi + guard + boş-binder temizliği); **P1-A, P2-C KAPATILDI**; **P2-A, P2-B belgelendi** (`CANONICAL-PATTERNS.md` canonical yollar); **P2-D belgelendi** (`ADOPTION.md` fork-sahiplenme). Ayrıca `.github/workflows/ci.yml` eklendi (GameCI EditMode+PlayMode testleri + doc/code tutarlılık guard'ı). EditMode 74/74 + PlayMode 56/57 (1 ignored) yeşil, Architecture Validation 0 Warning. Kalan tek "açık" tasarım kararı tam 1.0 stabilizasyonudur (ayrı sürüm).

---

## 🔴 P0-C — SignalBus Execution Order Bug → DÜZELTİLDİ ✅ (2026-07-15)

### Sorun
`SignalBus.FireInternal()` ve `FireInternalAsync()` içinde **subscriptions (mediator handler'lar) commands'tan ÖNCE** çalışıyordu. Bu, MVCS mimarisinin temel ilkesini ihlal ediyordu:
- Command: State değiştirir (Model)
- Subscription: Sonucu gözlemler (View/Mediator)

Ters sıralama yüzünden mediator'lar **eski (pre-command) model state** ile çalışıyordu. Örnek: Undo signal fire edildiğinde BoardMediator board'u eski haliyle rebuild ediyordu (halka 4. direkte kalıyordu), UndoCommand modeli düzeltmesine rağmen.

### Çözüm
`SignalBus.cs` `FireInternal` ve `FireInternalAsync` metodlarında execution order düzeltildi:

```
ÖNCE (YANLIŞ):                    SONRA (DOĞRU):
1. Interceptors                    1. Interceptors
2. Cross-Context                   2. Cross-Context
3. Subscriptions ← ESKİ STATE     3. Commands ← STATE DEĞİŞTİR
4. Commands                        4. Subscriptions ← YENİ STATE OKU
```

### Etki
- Tüm oyunlarda artık mediator subscribe handler'ları **her zaman güncel model state** okur
- "XCompletedSignal" gibi workaround pattern'lara gerek kalmaz
- Nexus ile geliştirilen her oyun bu execution order garantisinden otomatik yararlanır
- CHANGELOG v0.3.2 olarak kayıt altına alındı

